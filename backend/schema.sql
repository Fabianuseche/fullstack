-- usar este script para inicializar la base de datos

create table courses (
  id INT NOT NULL AUTO_INCREMENT,
  name text,
  schedule text,
  note FLOAT
);

create table students (

);

create table teachers (

);